// Test generated by RoostGPT for test go-unit-algo-string using AI Type Open AI and AI Model gpt-4

package LevenshteinDistance

import (
	"testing"
)

func TestLevenshteinDistance_c58c51e350(t *testing.T) {
	testCases := []struct {
		a        string
		b        string
		expected int
	}{
		{"kitten", "sitting", 3},
		{"", "test", 4},
		{"test", "", 4},
		{"test", "test", 0},
		{"longer string example", "short", 18},
		{"short", "longer string example", 18},
		{"special characters !@#", "sp3c1al charact3rs !@#", 4},
	}

	for _, tc := range testCases {
		result := levenshteinDistance(tc.a, tc.b)
		if result != tc.expected {
			t.Errorf("levenshteinDistance(%q, %q) = %v; want %v", tc.a, tc.b, result, tc.expected)
		}
	}
}

func min(x, y uint16) uint16 {
	if x < y {
		return x
	}
	return y
}
